# set the modes
shift + cmd - b                :   khd -e "mode activate prefix";\
				kwmc config border focused size 6
prefix + shift + cmd - b       :   khd -e "mode activate default";\
				kwmc config border focused size 1
# prefix + space		       :   khd -e "mode activate space"

# prefixes
prefix - return       :   open -a /Applications/iTerm.app;\
                            khd -e "mode activate default"

# Change the viewing mode
prefix - r            :   khd -e "reload" # reload config

prefix - a               :   kwmc space -t bsp;\
			     khd -e "mode activate default"
prefix - s               :   kwmc space -t monocle;\
			     khd -e "mode activate default"
prefix - d               :   kwmc space -t float;\
			     khd -e "mode activate default"

# set border color for different modes
khd mode default on_enter kwmc config border focused color 0xddd5c4a1
khd mode prefix on_enter kwmc config border focused color 0xddbdd322
#
# Change what window I'm working on
prefix + space - h            :   kwmc window -f west
prefix + space - l            :   kwmc window -f east
prefix + space - j            :   kwmc window -f south
prefix + space - k            :   kwmc window -f north
prefix + space - n            :   kwmc window -fm prev
prefix + space - m            :   kwmc window -fm next

# resize windows
prefix - h                :   kwmc window -c expand 0.05 west
prefix - k                :   kwmc window -c expand 0.05 north
prefix + shift - h        :   kwmc window -c reduce 0.05 west
prefix + shift - k        :   kwmc window -c reduce 0.05 north

# set border color for different modes
khd mode default on_enter kwmc config border focused color 0xddd5c4a1
khd mode prefix on_enter kwmc config border focused color 0xddbdd322
